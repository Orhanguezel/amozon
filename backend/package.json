{
  "name": "amozon-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun x tsc -p tsconfig.json && bun x tsc-alias -p tsconfig.json",
    "test": "bun test src/amazon/__tests__",
    "dev": "bun src/server.ts",
    "start": "bun src/server.ts",
    "scan": "bun src/run-job.ts",
    "db:schema": "bun src/apply-schema.ts",
    "demo:seed": "bun scripts/demo-seed.ts"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "drizzle-orm": "^0.44.6",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "@types/node": "20",
    "bun-types": "^1.3.1",
    "tsc-alias": "^1.8.16",
    "typescript": "5"
  }
}
